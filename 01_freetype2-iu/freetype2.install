post_install() {
  post_upgrade $1 0
}

post_upgrade() {
  if (( $(vercmp $2 2.6.5-1) < 0 )); then
    cat <<MSG
  Subpixel hinting now defaults to FT2_SUBPIXEL_HINTING=1. 
  It cannot be changed unless you recompile Freetype 2 
  without the Infinality patch bundle.

MSG
  fi
}
